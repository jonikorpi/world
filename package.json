{
  "name": "world",
  "version": "0.1.0",
  "private": true,
  "now": {
    "aliases": [
      "world-dev"
    ],
    "env": {
      "NODE_ENV": "production"
    }
  },
  "eslintConfig": {
    "extends": "react-app",
    "env": {
      "browser": true,
      "es6": true,
      "node": true
    }
  },
  "dependencies": {
    "body-parser": "^1.16.1",
    "express": "^4.14.1",
    "firebase": "^3.6.9",
    "firebase-admin": "^4.1.0",
    "jonikorpi-base-files": "^6.0.1",
    "lodash.throttle": "^4.1.1",
    "next": "^2.0.0-beta",
    "react-addons-perf": "^15.4.2",
    "react-mixin": "^3.0.5",
    "reactfire": "^1.0.0",
    "rollbar": "^0.6.3",
    "rollbar-browser": "^1.9.3",
    "tone": "^0.9.0",
    "whatwg-fetch": "^2.0.2"
  },
  "scripts": {
    "start": "firebase deploy && GAME_VERSION=`git log -n 1 --pretty=format:'%H'` nodemon --watch server --harmony-async-await server/server.js",
    "build": "next build",
    "test": "",
    "eject": "",
    "deploy-live": "now --alias -e GAME_VERSION=`git log -n 1 --pretty=format:'%H'` && curl https://api.rollbar.com/api/1/deploy/ -F access_token=22fca22d3936434eb8b69cc0c453d040 -F environment=production -F revision=`git log -n 1 --pretty=format:'%H'` -F local_username=`whoami` && firebase deploy",
    "deploy-testing": "now -e GAME_VERSION=`git log -n 1 --pretty=format:'%H'` && curl https://api.rollbar.com/api/1/deploy/ -F access_token=22fca22d3936434eb8b69cc0c453d040 -F environment=testing -F revision=`git log -n 1 --pretty=format:'%H'` -F local_username=`whoami` && firebase deploy",
    "prune": "now rm $(now-no-alias | json -a uid)",
    "now-start": "node --harmony-async-await server/server.js"
  },
  "devDependencies": {
    "babel-eslint": "^7.1.1",
    "eslint": "^3.15.0",
    "eslint-config-react-app": "^0.5.1",
    "eslint-plugin-flowtype": "^2.30.0",
    "eslint-plugin-import": "^2.2.0",
    "eslint-plugin-jsx-a11y": "^4.0.0",
    "eslint-plugin-react": "^6.9.0",
    "nodemon": "^1.11.0"
  }
}
